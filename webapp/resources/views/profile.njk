{% extends 'base' %}

{% block title %}
	Profile
{% endblock %}

{% block content %}
	{{ form.open({action: 'UserController.modify'}) }}

	{{ csrfField }}

	<fieldset>
		{{ form.label('Username') }}
		{{ form.text('username', currentUser.username, { disabled:"disabled" }) }}
	</fieldset>

	<fieldset>
		{{ form.label('Password') }}
		{{ form.password('password', null, {}) }}
	</fieldset>

	<fieldset>
		{{ form.label('Email') }}
		{{ form.email('email', currentUser.email, {}) }}
	</fieldset>

	{{ form.submit('Modify', 'modify', {}) }}

	{{ form.close() }}
{% endblock %}
